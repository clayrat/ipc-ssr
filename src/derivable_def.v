
(* For extracting a decision procedure which returns either a
   derivation or a countermodel, activate the following: *)

From ipcssr Require Export derivable_def_type.

(* OR *)

(* For extracting a decision procedure which returns either
   "derivable" or a countermodel, activate the following: *)

(* From ipcssr Require Export derivable_def_prop. *)